package com.chiefanalytics.interpreter.service;

public class TableInterpreter extends Interpreter {
    @Override
    protected byte[] interpret() {
        // TODO : Change
        // TODO : This is internal, getData() is used by other classes
        return new byte[0];
    }

    @Override
    public byte[] getData() {
        // TODO : Change
        return new byte[0];
    }
}
